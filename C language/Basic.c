#include<stdio.h>
int main()

/*  //...1...decimal to octal vice versa
{
    int num;
//    scanf("%o",&num);
//    printf("Octal number = %d",num);

    scanf("%d",&num);
    printf("Octal number = %o",num);
}

  //...2...decimal to hexadecimal vice versa
{
    int num;
//    scanf("%x",&num);
//    printf("Octal number = %d",num);

    scanf("%d",&num);
    printf("Octal number = %x",num);
}

  //...3...sum of digit
{
    int num,temp,r,sum=0;
    scanf("%d",&num);
    temp=num;
    while(temp!=0)
    {
        r=temp%10;
        sum=sum+r;
        temp=temp/10;
    }
    printf("sum of digit = %d",sum);

}

  //...4...Reverse
{
    int num,temp,r,sum=0;
    scanf("%d",&num);
    temp=num;
    while(temp!=0)
    {
        r=temp%10;
        sum=sum*10+r;
        temp=temp/10;
    }
    printf("sum of digit = %d",sum);

}
  //...5...Palindrome
{
    int num,temp,r,sum=0;
    scanf("%d",&num);
    temp=num;

    while(temp!=0)
    {
        r=temp%10;
        sum=sum*10+r;
        temp=temp/10;
    }
    printf("sum of digit = %d\n",sum);

    if(num==sum)
        printf("Palindrome");
    else
       printf("Not Palindrome");
}

  //...6...GCD &LCM
{
    int num1,num2,n1,n2,r,gcd,lcm;
    scanf("%d %d",&num1,&num2);
    n1=num1;
    n2=num2;
    while(n2!=0)
    {
        if(n1>n2)
        {
            r=n1%n2;
            n1=n2;
            n2=r;
        }
    }
    gcd=n1;
    lcm=(num1*num2)/gcd;

    printf("GCD = %d\n",gcd);
    printf("LCM = %d\n",lcm);

}
*/




